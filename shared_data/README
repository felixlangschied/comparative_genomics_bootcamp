This directory contains the final data generated by the 
different analyses.

Each final data file must have a corresponding 
metadatafile that informs about:
	* Variables
	* Path to the script that generated the data
	* Input data
